@model IEnumerable<PhotoContest.Infrastructure.Models.ViewModels.Invitation.NotificationViewModel>

@{
    foreach (var notification in @Model)
    {
        @Html.Partial("_Notification", notification)
    }
}